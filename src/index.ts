import { Query } from "./Analyzer"
import { Replace } from "./Entity"
import { Start } from "./Interface"

export const Analyzer = {
    Query: Query
};

export const Entity = {
    Replace: Replace
};

export const Interface = {
    Start: Start
};